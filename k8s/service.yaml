apiVersion: v1
kind: Service
metadata:
  name: k8s-website-service
  labels:
    app: k8s-website
spec:
  type: NodePort
  selector:
    app: k8s-website
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
